export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem" }}>
      <h1>Patient Flow Orchestrator</h1>
      <p>
        Simulator API is live at <code>/api/sim/state</code>. The bed-board UI lands in Phase 6.
      </p>
    </main>
  )
}
